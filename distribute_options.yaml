output: dist/
releases:
  - name: dev
    jobs:
      # Build and publish your ipa pkg to pgyer
      - name: release-dev-ios
        package:
          platform: windows
          target: msix
          # build_args:
          #   certificate-path: "dist/output.pfx"
          #   certificate-password: "123456"
        #     dart-define:
        #       APP_ENV: dev
        # publish_to: pgyer